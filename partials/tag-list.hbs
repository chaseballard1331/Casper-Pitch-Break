<ul class="sidebar-nav">
  {{#get 'tags' limit='all'}}
    {{#foreach tags}}
      <li><button class="sidebar-button tag-button" data-tag="{{slug}}">{{name}}</button></li>
    {{/foreach}}
  {{/get}}
</ul>